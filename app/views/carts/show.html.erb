<% if enhanced_cart.count > 0 %>
<%= render 'cart' %>
<% else %>
<h2>Sorry, Your Cart is empty.</h2>
<h3> Would you like to add something to it and then come back?</h3>
<%= link_to 'Home', :root %>
<% end %> 
